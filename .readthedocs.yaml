version: 2

submodules:
  exclude: all

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

python:
  install:
  # - requirements: docs/requirements.txt
  # - path: .
  - method: pip
    path: .
    extra_requirements:
      - doc
